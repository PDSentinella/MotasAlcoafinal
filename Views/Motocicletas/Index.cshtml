@model List<motasAlcoafinal.Models.Motocicletas>
@{
    ViewData["Title"] = "Lista de Motocicletas";
}

<div class="d-flex justify-content-between align-items-center">
    <h2>Lista de Motocicletas</h2>
    <a class="btn btn-primary" asp-controller="Motocicletas" asp-action="Create">Criar Motocicleta</a>
</div>

<form asp-controller="Motocicletas" asp-action="Index" method="get" class="mb-3">
    <div class="input-group">
        <input type="text" name="searchString" value="@ViewBag.SearchString" class="form-control" placeholder="Pesquisar motocicletas..." />
        <button type="submit" class="btn btn-primary">Pesquisar</button>
    </div>
</form>

<table class="table table-striped table-responsive">
    <thead>
        <tr>
            <th>Marca</th>
            <th>Modelo</th>
            <th>Ano</th>
            <th>Placa</th>
            <th>Cliente</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var motocicleta in Model)
        {
            <tr>
                <td>@motocicleta.Marca</td>
                <td>@motocicleta.Modelo</td>
                <td>@motocicleta.Ano</td>
                <td>@motocicleta.Placa</td>
                <td>@motocicleta.Cliente?.Nome</td>
                <td>
                    <a asp-action="Details" asp-route-id="@motocicleta.Id" class="btn btn-info">Detalhes</a>
                    <a asp-action="Edit" asp-route-id="@motocicleta.Id" class="btn btn-warning">Editar</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<nav aria-label="Page navigation">
    <ul class="pagination justify-content-center">
        <li class="page-item @(ViewBag.CurrentPage == 1 ? "disabled" : "")">
            <a class="page-link" asp-controller="Motocicletas" asp-action="Index" asp-route-pageNumber="@(ViewBag.CurrentPage - 1)" asp-route-searchString="@ViewBag.SearchString">&#9664;</a>
        </li>
        <li class="page-item disabled">
            <span class="page-link">@ViewBag.CurrentPage</span>
        </li>
        <li class="page-item @(ViewBag.CurrentPage == ViewBag.TotalPages ? "disabled" : "")">
            <a class="page-link" asp-controller="Motocicletas" asp-action="Index" asp-route-pageNumber="@(ViewBag.CurrentPage + 1)" asp-route-searchString="@ViewBag.SearchString">&#9654;</a>
        </li>
    </ul>
</nav>